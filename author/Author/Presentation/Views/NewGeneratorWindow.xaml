<Window x:Class="author.Presentation.Views.NewGeneratorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="新建数据生成器" Height="280" Width="440"
        WindowStartupLocation="CenterOwner" FontSize="13" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="名称：" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="NameBox" Grid.Column="1" VerticalContentAlignment="Center"
                     ToolTip="生成器名称（字母/数字/下划线/横线，如 juhua）"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="描述：" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="DescBox" Grid.Column="1" VerticalContentAlignment="Center"
                     ToolTip="自定义描述（如「菊花图生成器」），可留空"/>
        </Grid>

        <TextBlock Grid.Row="2" Foreground="#888" TextWrapping="Wrap" Margin="0,10,0,0"
                   Text="名称不能为空、不能为「无」，且不能与已有生成器重名。"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="保存" Width="90" IsDefault="True" Click="OnSave"/>
            <Button Content="取消" Width="80" Margin="8,0,0,0" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
